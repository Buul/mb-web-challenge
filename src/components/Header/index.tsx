import { FC } from 'react';

export const Header: FC = () => (
  <header className="shadow-lg w-full h-20 px-5 flex items-center">
    <img src="/logo-icon-color.svg" alt="Logotipo MB" width={50} />
  </header>
);
